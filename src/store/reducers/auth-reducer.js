

const initialState = {
    authenticated: false
}

const taskReducer = ( state = initialState, action ) => {
    switch ( action.type ) {
        case 'REGISTER_ACCOUNT':
            return state
    }
    return state;
};

export default taskReducer;